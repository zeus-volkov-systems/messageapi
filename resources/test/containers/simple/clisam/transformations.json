{
    "transformations": [
        {
            "id": "join-test",
            "operator": "join",
            "fields": ["key", "record", "filename", "type", "mix-and-match"],
            "parameters": {
                "join_field": "key",
                "new_field": "mix-and-match"
            },
            "records" : {"parent": {"CLASSIFIER": ["namespace", "condition-test"]},
                         "child":  {"COLLECTION": "mix-and-match"}}
        },
        {
            "id": "reduce-test",
            "operator": "reduce-sum",
            "fields": ["key", "mix-and-match-sum"],
            "parameters": {
                "reduce-field": "mix-and-match"
            },
            "records": {"collection" : {"TRANSFORMATION": "join-test"}}
        }
    ]
}
